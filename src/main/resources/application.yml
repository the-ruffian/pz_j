spring:
  thymeleaf:
    cache: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://106.13.171.218/YY?userSSL=false
    username: root
    password: hl951103
    # 初始化时运行sql脚本
    schema: classpath:sql/schema.sql
    initialization-mode: always
server:
  port: 80
logging:
  level:
    root: info
    com.example: debug
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl